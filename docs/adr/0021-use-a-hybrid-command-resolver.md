# Use a hybrid command resolver

Command State detects reserved safety controls locally, resolves exact Command Vocabulary and Voice Routine phrases deterministically, and uses the selected Theo Model only for mapping free-form language into cataloged Voice Actions and parameters. The model cannot invent actions or bypass availability, confidence, trust, or confirmation gates. This avoids a fourth model setting while keeping safety controls fast, routine commands predictable, and natural phrasing flexible.
